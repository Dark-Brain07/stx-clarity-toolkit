import{describe,it,expect}from"vitest";import{MempoolClient}from"../../api/client/MempoolClient";
describe("MempoolClient",()=>{it("creates",()=>expect(new MempoolClient()).toBeDefined());it("state",()=>{const x=new MempoolClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MempoolClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MempoolClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MempoolClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MempoolClient();expect(x.toJSON().active).toBe(true);});});