import{describe,it,expect}from"vitest";import{HiroClient}from"../../api/client/HiroClient";
describe("HiroClient",()=>{it("creates",()=>expect(new HiroClient()).toBeDefined());it("state",()=>{const x=new HiroClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new HiroClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new HiroClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new HiroClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new HiroClient();expect(x.toJSON().active).toBe(true);});});