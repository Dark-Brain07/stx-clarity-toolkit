import{describe,it,expect}from"vitest";import{WebSocketClient}from"../../api/client/WebSocketClient";
describe("WebSocketClient",()=>{it("creates",()=>expect(new WebSocketClient()).toBeDefined());it("state",()=>{const x=new WebSocketClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WebSocketClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WebSocketClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WebSocketClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WebSocketClient();expect(x.toJSON().active).toBe(true);});});