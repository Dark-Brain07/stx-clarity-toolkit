import{describe,it,expect}from"vitest";import{VelarClient}from"../../api/client/VelarClient";
describe("VelarClient",()=>{it("creates",()=>expect(new VelarClient()).toBeDefined());it("state",()=>{const x=new VelarClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new VelarClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new VelarClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new VelarClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new VelarClient();expect(x.toJSON().active).toBe(true);});});