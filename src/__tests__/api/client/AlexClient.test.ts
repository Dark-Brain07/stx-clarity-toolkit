import{describe,it,expect}from"vitest";import{AlexClient}from"../../api/client/AlexClient";
describe("AlexClient",()=>{it("creates",()=>expect(new AlexClient()).toBeDefined());it("state",()=>{const x=new AlexClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AlexClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AlexClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AlexClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AlexClient();expect(x.toJSON().active).toBe(true);});});