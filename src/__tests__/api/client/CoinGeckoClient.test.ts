import{describe,it,expect}from"vitest";import{CoinGeckoClient}from"../../api/client/CoinGeckoClient";
describe("CoinGeckoClient",()=>{it("creates",()=>expect(new CoinGeckoClient()).toBeDefined());it("state",()=>{const x=new CoinGeckoClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new CoinGeckoClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new CoinGeckoClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new CoinGeckoClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new CoinGeckoClient();expect(x.toJSON().active).toBe(true);});});