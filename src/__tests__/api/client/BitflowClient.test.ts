import{describe,it,expect}from"vitest";import{BitflowClient}from"../../api/client/BitflowClient";
describe("BitflowClient",()=>{it("creates",()=>expect(new BitflowClient()).toBeDefined());it("state",()=>{const x=new BitflowClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BitflowClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BitflowClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BitflowClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BitflowClient();expect(x.toJSON().active).toBe(true);});});