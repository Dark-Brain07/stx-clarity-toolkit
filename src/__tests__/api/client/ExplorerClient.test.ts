import{describe,it,expect}from"vitest";import{ExplorerClient}from"../../api/client/ExplorerClient";
describe("ExplorerClient",()=>{it("creates",()=>expect(new ExplorerClient()).toBeDefined());it("state",()=>{const x=new ExplorerClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ExplorerClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ExplorerClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ExplorerClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ExplorerClient();expect(x.toJSON().active).toBe(true);});});