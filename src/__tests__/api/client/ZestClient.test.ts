import{describe,it,expect}from"vitest";import{ZestClient}from"../../api/client/ZestClient";
describe("ZestClient",()=>{it("creates",()=>expect(new ZestClient()).toBeDefined());it("state",()=>{const x=new ZestClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ZestClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ZestClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ZestClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ZestClient();expect(x.toJSON().active).toBe(true);});});