import{describe,it,expect}from"vitest";import{StacksClient}from"../../api/client/StacksClient";
describe("StacksClient",()=>{it("creates",()=>expect(new StacksClient()).toBeDefined());it("state",()=>{const x=new StacksClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new StacksClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new StacksClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new StacksClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new StacksClient();expect(x.toJSON().active).toBe(true);});});