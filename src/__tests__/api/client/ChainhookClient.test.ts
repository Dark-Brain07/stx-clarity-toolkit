import{describe,it,expect}from"vitest";import{ChainhookClient}from"../../api/client/ChainhookClient";
describe("ChainhookClient",()=>{it("creates",()=>expect(new ChainhookClient()).toBeDefined());it("state",()=>{const x=new ChainhookClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ChainhookClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ChainhookClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ChainhookClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ChainhookClient();expect(x.toJSON().active).toBe(true);});});