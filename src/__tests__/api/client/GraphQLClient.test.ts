import{describe,it,expect}from"vitest";import{GraphQLClient}from"../../api/client/GraphQLClient";
describe("GraphQLClient",()=>{it("creates",()=>expect(new GraphQLClient()).toBeDefined());it("state",()=>{const x=new GraphQLClient();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new GraphQLClient();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new GraphQLClient();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new GraphQLClient();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new GraphQLClient();expect(x.toJSON().active).toBe(true);});});