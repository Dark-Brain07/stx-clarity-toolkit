import{describe,it,expect}from"vitest";import{Vault}from"../../api/models/Vault";
describe("Vault",()=>{it("creates",()=>expect(new Vault()).toBeDefined());it("state",()=>{const x=new Vault();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Vault();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Vault();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Vault();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Vault();expect(x.toJSON().active).toBe(true);});});