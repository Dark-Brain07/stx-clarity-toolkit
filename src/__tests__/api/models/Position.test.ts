import{describe,it,expect}from"vitest";import{Position}from"../../api/models/Position";
describe("Position",()=>{it("creates",()=>expect(new Position()).toBeDefined());it("state",()=>{const x=new Position();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Position();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Position();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Position();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Position();expect(x.toJSON().active).toBe(true);});});