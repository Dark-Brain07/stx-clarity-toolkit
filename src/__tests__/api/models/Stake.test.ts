import{describe,it,expect}from"vitest";import{Stake}from"../../api/models/Stake";
describe("Stake",()=>{it("creates",()=>expect(new Stake()).toBeDefined());it("state",()=>{const x=new Stake();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Stake();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Stake();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Stake();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Stake();expect(x.toJSON().active).toBe(true);});});