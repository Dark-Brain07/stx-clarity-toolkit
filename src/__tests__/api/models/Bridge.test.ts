import{describe,it,expect}from"vitest";import{Bridge}from"../../api/models/Bridge";
describe("Bridge",()=>{it("creates",()=>expect(new Bridge()).toBeDefined());it("state",()=>{const x=new Bridge();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Bridge();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Bridge();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Bridge();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Bridge();expect(x.toJSON().active).toBe(true);});});