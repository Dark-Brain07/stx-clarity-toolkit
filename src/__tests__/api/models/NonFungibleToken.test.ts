import{describe,it,expect}from"vitest";import{NonFungibleToken}from"../../api/models/NonFungibleToken";
describe("NonFungibleToken",()=>{it("creates",()=>expect(new NonFungibleToken()).toBeDefined());it("state",()=>{const x=new NonFungibleToken();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NonFungibleToken();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NonFungibleToken();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NonFungibleToken();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NonFungibleToken();expect(x.toJSON().active).toBe(true);});});