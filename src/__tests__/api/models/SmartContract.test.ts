import{describe,it,expect}from"vitest";import{SmartContract}from"../../api/models/SmartContract";
describe("SmartContract",()=>{it("creates",()=>expect(new SmartContract()).toBeDefined());it("state",()=>{const x=new SmartContract();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SmartContract();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SmartContract();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SmartContract();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SmartContract();expect(x.toJSON().active).toBe(true);});});