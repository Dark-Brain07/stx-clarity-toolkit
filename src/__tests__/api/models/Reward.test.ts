import{describe,it,expect}from"vitest";import{Reward}from"../../api/models/Reward";
describe("Reward",()=>{it("creates",()=>expect(new Reward()).toBeDefined());it("state",()=>{const x=new Reward();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Reward();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Reward();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Reward();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Reward();expect(x.toJSON().active).toBe(true);});});