import{describe,it,expect}from"vitest";import{Proposal}from"../../api/models/Proposal";
describe("Proposal",()=>{it("creates",()=>expect(new Proposal()).toBeDefined());it("state",()=>{const x=new Proposal();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Proposal();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Proposal();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Proposal();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Proposal();expect(x.toJSON().active).toBe(true);});});