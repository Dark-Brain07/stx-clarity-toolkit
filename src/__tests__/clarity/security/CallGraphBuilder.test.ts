import{describe,it,expect}from"vitest";import{CallGraphBuilder}from"../../clarity/security/CallGraphBuilder";
describe("CallGraphBuilder",()=>{it("creates",()=>expect(new CallGraphBuilder()).toBeDefined());it("state",()=>{const x=new CallGraphBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new CallGraphBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new CallGraphBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new CallGraphBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new CallGraphBuilder();expect(x.toJSON().active).toBe(true);});});