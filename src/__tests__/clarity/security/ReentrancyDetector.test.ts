import{describe,it,expect}from"vitest";import{ReentrancyDetector}from"../../clarity/security/ReentrancyDetector";
describe("ReentrancyDetector",()=>{it("creates",()=>expect(new ReentrancyDetector()).toBeDefined());it("state",()=>{const x=new ReentrancyDetector();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ReentrancyDetector();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ReentrancyDetector();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ReentrancyDetector();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ReentrancyDetector();expect(x.toJSON().active).toBe(true);});});