import{describe,it,expect}from"vitest";import{SecurityScanner}from"../../clarity/security/SecurityScanner";
describe("SecurityScanner",()=>{it("creates",()=>expect(new SecurityScanner()).toBeDefined());it("state",()=>{const x=new SecurityScanner();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SecurityScanner();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SecurityScanner();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SecurityScanner();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SecurityScanner();expect(x.toJSON().active).toBe(true);});});