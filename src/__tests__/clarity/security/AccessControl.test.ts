import{describe,it,expect}from"vitest";import{AccessControl}from"../../clarity/security/AccessControl";
describe("AccessControl",()=>{it("creates",()=>expect(new AccessControl()).toBeDefined());it("state",()=>{const x=new AccessControl();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AccessControl();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AccessControl();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AccessControl();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AccessControl();expect(x.toJSON().active).toBe(true);});});