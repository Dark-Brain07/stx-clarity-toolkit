import{describe,it,expect}from"vitest";import{OracleManipDetect}from"../../clarity/security/OracleManipDetect";
describe("OracleManipDetect",()=>{it("creates",()=>expect(new OracleManipDetect()).toBeDefined());it("state",()=>{const x=new OracleManipDetect();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new OracleManipDetect();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new OracleManipDetect();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new OracleManipDetect();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new OracleManipDetect();expect(x.toJSON().active).toBe(true);});});