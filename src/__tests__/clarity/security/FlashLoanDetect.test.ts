import{describe,it,expect}from"vitest";import{FlashLoanDetect}from"../../clarity/security/FlashLoanDetect";
describe("FlashLoanDetect",()=>{it("creates",()=>expect(new FlashLoanDetect()).toBeDefined());it("state",()=>{const x=new FlashLoanDetect();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FlashLoanDetect();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FlashLoanDetect();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FlashLoanDetect();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FlashLoanDetect();expect(x.toJSON().active).toBe(true);});});