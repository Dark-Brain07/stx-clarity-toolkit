import{describe,it,expect}from"vitest";import{FrontRunDetect}from"../../clarity/security/FrontRunDetect";
describe("FrontRunDetect",()=>{it("creates",()=>expect(new FrontRunDetect()).toBeDefined());it("state",()=>{const x=new FrontRunDetect();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FrontRunDetect();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FrontRunDetect();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FrontRunDetect();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FrontRunDetect();expect(x.toJSON().active).toBe(true);});});