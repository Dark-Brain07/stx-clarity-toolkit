import{describe,it,expect}from"vitest";import{PrincipalValidator}from"../../clarity/parser/PrincipalValidator";
describe("PrincipalValidator",()=>{it("creates",()=>expect(new PrincipalValidator()).toBeDefined());it("state",()=>{const x=new PrincipalValidator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PrincipalValidator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PrincipalValidator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PrincipalValidator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PrincipalValidator();expect(x.toJSON().active).toBe(true);});});