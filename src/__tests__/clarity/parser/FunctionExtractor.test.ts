import{describe,it,expect}from"vitest";import{FunctionExtractor}from"../../clarity/parser/FunctionExtractor";
describe("FunctionExtractor",()=>{it("creates",()=>expect(new FunctionExtractor()).toBeDefined());it("state",()=>{const x=new FunctionExtractor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FunctionExtractor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FunctionExtractor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FunctionExtractor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FunctionExtractor();expect(x.toJSON().active).toBe(true);});});