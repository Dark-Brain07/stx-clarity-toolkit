import{describe,it,expect}from"vitest";import{ImportResolver}from"../../clarity/parser/ImportResolver";
describe("ImportResolver",()=>{it("creates",()=>expect(new ImportResolver()).toBeDefined());it("state",()=>{const x=new ImportResolver();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ImportResolver();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ImportResolver();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ImportResolver();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ImportResolver();expect(x.toJSON().active).toBe(true);});});