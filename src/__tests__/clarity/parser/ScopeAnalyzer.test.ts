import{describe,it,expect}from"vitest";import{ScopeAnalyzer}from"../../clarity/parser/ScopeAnalyzer";
describe("ScopeAnalyzer",()=>{it("creates",()=>expect(new ScopeAnalyzer()).toBeDefined());it("state",()=>{const x=new ScopeAnalyzer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ScopeAnalyzer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ScopeAnalyzer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ScopeAnalyzer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ScopeAnalyzer();expect(x.toJSON().active).toBe(true);});});