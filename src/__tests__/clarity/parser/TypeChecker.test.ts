import{describe,it,expect}from"vitest";import{TypeChecker}from"../../clarity/parser/TypeChecker";
describe("TypeChecker",()=>{it("creates",()=>expect(new TypeChecker()).toBeDefined());it("state",()=>{const x=new TypeChecker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TypeChecker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TypeChecker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TypeChecker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TypeChecker();expect(x.toJSON().active).toBe(true);});});