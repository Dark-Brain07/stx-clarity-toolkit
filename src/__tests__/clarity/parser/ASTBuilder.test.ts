import{describe,it,expect}from"vitest";import{ASTBuilder}from"../../clarity/parser/ASTBuilder";
describe("ASTBuilder",()=>{it("creates",()=>expect(new ASTBuilder()).toBeDefined());it("state",()=>{const x=new ASTBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ASTBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ASTBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ASTBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ASTBuilder();expect(x.toJSON().active).toBe(true);});});