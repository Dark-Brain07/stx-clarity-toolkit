import{describe,it,expect}from"vitest";import{ClarityParser}from"../../clarity/parser/ClarityParser";
describe("ClarityParser",()=>{it("creates",()=>expect(new ClarityParser()).toBeDefined());it("state",()=>{const x=new ClarityParser();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ClarityParser();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ClarityParser();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ClarityParser();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ClarityParser();expect(x.toJSON().active).toBe(true);});});