import{describe,it,expect}from"vitest";import{ErrorCollector}from"../../clarity/parser/ErrorCollector";
describe("ErrorCollector",()=>{it("creates",()=>expect(new ErrorCollector()).toBeDefined());it("state",()=>{const x=new ErrorCollector();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ErrorCollector();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ErrorCollector();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ErrorCollector();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ErrorCollector();expect(x.toJSON().active).toBe(true);});});