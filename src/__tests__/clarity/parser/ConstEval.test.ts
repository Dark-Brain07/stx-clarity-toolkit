import{describe,it,expect}from"vitest";import{ConstEval}from"../../clarity/parser/ConstEval";
describe("ConstEval",()=>{it("creates",()=>expect(new ConstEval()).toBeDefined());it("state",()=>{const x=new ConstEval();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ConstEval();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ConstEval();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ConstEval();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ConstEval();expect(x.toJSON().active).toBe(true);});});