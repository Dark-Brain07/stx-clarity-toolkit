import{describe,it,expect}from"vitest";import{ResponseChecker}from"../../clarity/parser/ResponseChecker";
describe("ResponseChecker",()=>{it("creates",()=>expect(new ResponseChecker()).toBeDefined());it("state",()=>{const x=new ResponseChecker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ResponseChecker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ResponseChecker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ResponseChecker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ResponseChecker();expect(x.toJSON().active).toBe(true);});});