import{describe,it,expect}from"vitest";import{MapAnalyzer}from"../../clarity/parser/MapAnalyzer";
describe("MapAnalyzer",()=>{it("creates",()=>expect(new MapAnalyzer()).toBeDefined());it("state",()=>{const x=new MapAnalyzer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MapAnalyzer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MapAnalyzer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MapAnalyzer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MapAnalyzer();expect(x.toJSON().active).toBe(true);});});