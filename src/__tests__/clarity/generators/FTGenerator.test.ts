import{describe,it,expect}from"vitest";import{FTGenerator}from"../../clarity/generators/FTGenerator";
describe("FTGenerator",()=>{it("creates",()=>expect(new FTGenerator()).toBeDefined());it("state",()=>{const x=new FTGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FTGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FTGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FTGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FTGenerator();expect(x.toJSON().active).toBe(true);});});