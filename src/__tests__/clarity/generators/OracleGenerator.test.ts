import{describe,it,expect}from"vitest";import{OracleGenerator}from"../../clarity/generators/OracleGenerator";
describe("OracleGenerator",()=>{it("creates",()=>expect(new OracleGenerator()).toBeDefined());it("state",()=>{const x=new OracleGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new OracleGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new OracleGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new OracleGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new OracleGenerator();expect(x.toJSON().active).toBe(true);});});