import{describe,it,expect}from"vitest";import{StakingGenerator}from"../../clarity/generators/StakingGenerator";
describe("StakingGenerator",()=>{it("creates",()=>expect(new StakingGenerator()).toBeDefined());it("state",()=>{const x=new StakingGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new StakingGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new StakingGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new StakingGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new StakingGenerator();expect(x.toJSON().active).toBe(true);});});