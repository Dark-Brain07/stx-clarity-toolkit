import{describe,it,expect}from"vitest";import{MultiSigGenerator}from"../../clarity/generators/MultiSigGenerator";
describe("MultiSigGenerator",()=>{it("creates",()=>expect(new MultiSigGenerator()).toBeDefined());it("state",()=>{const x=new MultiSigGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MultiSigGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MultiSigGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MultiSigGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MultiSigGenerator();expect(x.toJSON().active).toBe(true);});});