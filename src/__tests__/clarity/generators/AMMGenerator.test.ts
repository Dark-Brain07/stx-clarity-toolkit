import{describe,it,expect}from"vitest";import{AMMGenerator}from"../../clarity/generators/AMMGenerator";
describe("AMMGenerator",()=>{it("creates",()=>expect(new AMMGenerator()).toBeDefined());it("state",()=>{const x=new AMMGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AMMGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AMMGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AMMGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AMMGenerator();expect(x.toJSON().active).toBe(true);});});