import{describe,it,expect}from"vitest";import{LendingGenerator}from"../../clarity/generators/LendingGenerator";
describe("LendingGenerator",()=>{it("creates",()=>expect(new LendingGenerator()).toBeDefined());it("state",()=>{const x=new LendingGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LendingGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new LendingGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new LendingGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new LendingGenerator();expect(x.toJSON().active).toBe(true);});});