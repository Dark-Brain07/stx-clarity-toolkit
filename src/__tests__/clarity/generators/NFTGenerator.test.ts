import{describe,it,expect}from"vitest";import{NFTGenerator}from"../../clarity/generators/NFTGenerator";
describe("NFTGenerator",()=>{it("creates",()=>expect(new NFTGenerator()).toBeDefined());it("state",()=>{const x=new NFTGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NFTGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NFTGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NFTGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NFTGenerator();expect(x.toJSON().active).toBe(true);});});