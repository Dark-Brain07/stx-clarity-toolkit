import{describe,it,expect}from"vitest";import{VaultGenerator}from"../../clarity/generators/VaultGenerator";
describe("VaultGenerator",()=>{it("creates",()=>expect(new VaultGenerator()).toBeDefined());it("state",()=>{const x=new VaultGenerator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new VaultGenerator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new VaultGenerator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new VaultGenerator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new VaultGenerator();expect(x.toJSON().active).toBe(true);});});