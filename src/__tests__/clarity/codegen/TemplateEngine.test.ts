import{describe,it,expect}from"vitest";import{TemplateEngine}from"../../clarity/codegen/TemplateEngine";
describe("TemplateEngine",()=>{it("creates",()=>expect(new TemplateEngine()).toBeDefined());it("state",()=>{const x=new TemplateEngine();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TemplateEngine();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TemplateEngine();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TemplateEngine();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TemplateEngine();expect(x.toJSON().active).toBe(true);});});