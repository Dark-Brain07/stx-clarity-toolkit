import{describe,it,expect}from"vitest";import{FunctionBuilder}from"../../clarity/codegen/FunctionBuilder";
describe("FunctionBuilder",()=>{it("creates",()=>expect(new FunctionBuilder()).toBeDefined());it("state",()=>{const x=new FunctionBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FunctionBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FunctionBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FunctionBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FunctionBuilder();expect(x.toJSON().active).toBe(true);});});