import{describe,it,expect}from"vitest";import{MapBuilder}from"../../clarity/codegen/MapBuilder";
describe("MapBuilder",()=>{it("creates",()=>expect(new MapBuilder()).toBeDefined());it("state",()=>{const x=new MapBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MapBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MapBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MapBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MapBuilder();expect(x.toJSON().active).toBe(true);});});