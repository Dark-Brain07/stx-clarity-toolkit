import{describe,it,expect}from"vitest";import{ContractBuilder}from"../../clarity/codegen/ContractBuilder";
describe("ContractBuilder",()=>{it("creates",()=>expect(new ContractBuilder()).toBeDefined());it("state",()=>{const x=new ContractBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ContractBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ContractBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ContractBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ContractBuilder();expect(x.toJSON().active).toBe(true);});});