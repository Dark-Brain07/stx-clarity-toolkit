import{describe,it,expect}from"vitest";import{EventBuilder}from"../../clarity/codegen/EventBuilder";
describe("EventBuilder",()=>{it("creates",()=>expect(new EventBuilder()).toBeDefined());it("state",()=>{const x=new EventBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new EventBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new EventBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new EventBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new EventBuilder();expect(x.toJSON().active).toBe(true);});});