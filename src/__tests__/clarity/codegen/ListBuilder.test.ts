import{describe,it,expect}from"vitest";import{ListBuilder}from"../../clarity/codegen/ListBuilder";
describe("ListBuilder",()=>{it("creates",()=>expect(new ListBuilder()).toBeDefined());it("state",()=>{const x=new ListBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ListBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ListBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ListBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ListBuilder();expect(x.toJSON().active).toBe(true);});});