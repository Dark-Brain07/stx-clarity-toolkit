import{describe,it,expect}from"vitest";import{PrincipalBuilder}from"../../clarity/codegen/PrincipalBuilder";
describe("PrincipalBuilder",()=>{it("creates",()=>expect(new PrincipalBuilder()).toBeDefined());it("state",()=>{const x=new PrincipalBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PrincipalBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PrincipalBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PrincipalBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PrincipalBuilder();expect(x.toJSON().active).toBe(true);});});