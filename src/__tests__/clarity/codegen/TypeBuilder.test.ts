import{describe,it,expect}from"vitest";import{TypeBuilder}from"../../clarity/codegen/TypeBuilder";
describe("TypeBuilder",()=>{it("creates",()=>expect(new TypeBuilder()).toBeDefined());it("state",()=>{const x=new TypeBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TypeBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TypeBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TypeBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TypeBuilder();expect(x.toJSON().active).toBe(true);});});