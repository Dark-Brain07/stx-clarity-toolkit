import{describe,it,expect}from"vitest";import{BufferBuilder}from"../../clarity/codegen/BufferBuilder";
describe("BufferBuilder",()=>{it("creates",()=>expect(new BufferBuilder()).toBeDefined());it("state",()=>{const x=new BufferBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BufferBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BufferBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BufferBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BufferBuilder();expect(x.toJSON().active).toBe(true);});});