import{describe,it,expect}from"vitest";import{ConstBuilder}from"../../clarity/codegen/ConstBuilder";
describe("ConstBuilder",()=>{it("creates",()=>expect(new ConstBuilder()).toBeDefined());it("state",()=>{const x=new ConstBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ConstBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ConstBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ConstBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ConstBuilder();expect(x.toJSON().active).toBe(true);});});