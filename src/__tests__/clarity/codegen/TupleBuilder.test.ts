import{describe,it,expect}from"vitest";import{TupleBuilder}from"../../clarity/codegen/TupleBuilder";
describe("TupleBuilder",()=>{it("creates",()=>expect(new TupleBuilder()).toBeDefined());it("state",()=>{const x=new TupleBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TupleBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TupleBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TupleBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TupleBuilder();expect(x.toJSON().active).toBe(true);});});