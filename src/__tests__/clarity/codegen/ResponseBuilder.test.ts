import{describe,it,expect}from"vitest";import{ResponseBuilder}from"../../clarity/codegen/ResponseBuilder";
describe("ResponseBuilder",()=>{it("creates",()=>expect(new ResponseBuilder()).toBeDefined());it("state",()=>{const x=new ResponseBuilder();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ResponseBuilder();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ResponseBuilder();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ResponseBuilder();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ResponseBuilder();expect(x.toJSON().active).toBe(true);});});