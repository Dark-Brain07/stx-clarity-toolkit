import{describe,it,expect}from"vitest";import{StringUtils}from"../../clarity/stdlib/StringUtils";
describe("StringUtils",()=>{it("creates",()=>expect(new StringUtils()).toBeDefined());it("state",()=>{const x=new StringUtils();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new StringUtils();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new StringUtils();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new StringUtils();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new StringUtils();expect(x.toJSON().active).toBe(true);});});