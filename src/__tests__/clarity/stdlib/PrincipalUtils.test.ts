import{describe,it,expect}from"vitest";import{PrincipalUtils}from"../../clarity/stdlib/PrincipalUtils";
describe("PrincipalUtils",()=>{it("creates",()=>expect(new PrincipalUtils()).toBeDefined());it("state",()=>{const x=new PrincipalUtils();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PrincipalUtils();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PrincipalUtils();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PrincipalUtils();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PrincipalUtils();expect(x.toJSON().active).toBe(true);});});