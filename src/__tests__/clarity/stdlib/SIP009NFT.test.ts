import{describe,it,expect}from"vitest";import{SIP009NFT}from"../../clarity/stdlib/SIP009NFT";
describe("SIP009NFT",()=>{it("creates",()=>expect(new SIP009NFT()).toBeDefined());it("state",()=>{const x=new SIP009NFT();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SIP009NFT();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SIP009NFT();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SIP009NFT();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SIP009NFT();expect(x.toJSON().active).toBe(true);});});