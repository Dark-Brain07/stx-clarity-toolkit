import{describe,it,expect}from"vitest";import{MultiSig}from"../../clarity/stdlib/MultiSig";
describe("MultiSig",()=>{it("creates",()=>expect(new MultiSig()).toBeDefined());it("state",()=>{const x=new MultiSig();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MultiSig();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MultiSig();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MultiSig();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MultiSig();expect(x.toJSON().active).toBe(true);});});