import{describe,it,expect}from"vitest";import{ReentrancyGuard}from"../../clarity/stdlib/ReentrancyGuard";
describe("ReentrancyGuard",()=>{it("creates",()=>expect(new ReentrancyGuard()).toBeDefined());it("state",()=>{const x=new ReentrancyGuard();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ReentrancyGuard();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ReentrancyGuard();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ReentrancyGuard();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ReentrancyGuard();expect(x.toJSON().active).toBe(true);});});