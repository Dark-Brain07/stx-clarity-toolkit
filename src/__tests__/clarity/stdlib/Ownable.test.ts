import{describe,it,expect}from"vitest";import{Ownable}from"../../clarity/stdlib/Ownable";
describe("Ownable",()=>{it("creates",()=>expect(new Ownable()).toBeDefined());it("state",()=>{const x=new Ownable();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Ownable();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Ownable();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Ownable();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Ownable();expect(x.toJSON().active).toBe(true);});});