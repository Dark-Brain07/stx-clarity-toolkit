import{describe,it,expect}from"vitest";import{SafeTransfer}from"../../clarity/stdlib/SafeTransfer";
describe("SafeTransfer",()=>{it("creates",()=>expect(new SafeTransfer()).toBeDefined());it("state",()=>{const x=new SafeTransfer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SafeTransfer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SafeTransfer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SafeTransfer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SafeTransfer();expect(x.toJSON().active).toBe(true);});});