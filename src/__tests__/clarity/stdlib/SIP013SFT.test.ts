import{describe,it,expect}from"vitest";import{SIP013SFT}from"../../clarity/stdlib/SIP013SFT";
describe("SIP013SFT",()=>{it("creates",()=>expect(new SIP013SFT()).toBeDefined());it("state",()=>{const x=new SIP013SFT();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SIP013SFT();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SIP013SFT();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SIP013SFT();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SIP013SFT();expect(x.toJSON().active).toBe(true);});});