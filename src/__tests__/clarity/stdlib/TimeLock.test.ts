import{describe,it,expect}from"vitest";import{TimeLock}from"../../clarity/stdlib/TimeLock";
describe("TimeLock",()=>{it("creates",()=>expect(new TimeLock()).toBeDefined());it("state",()=>{const x=new TimeLock();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TimeLock();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TimeLock();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TimeLock();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TimeLock();expect(x.toJSON().active).toBe(true);});});