import{describe,it,expect}from"vitest";import{AccessControlRole}from"../../clarity/stdlib/AccessControlRole";
describe("AccessControlRole",()=>{it("creates",()=>expect(new AccessControlRole()).toBeDefined());it("state",()=>{const x=new AccessControlRole();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AccessControlRole();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AccessControlRole();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AccessControlRole();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AccessControlRole();expect(x.toJSON().active).toBe(true);});});