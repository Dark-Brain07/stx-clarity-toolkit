import{describe,it,expect}from"vitest";import{Proxy}from"../../clarity/stdlib/Proxy";
describe("Proxy",()=>{it("creates",()=>expect(new Proxy()).toBeDefined());it("state",()=>{const x=new Proxy();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Proxy();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Proxy();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Proxy();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Proxy();expect(x.toJSON().active).toBe(true);});});