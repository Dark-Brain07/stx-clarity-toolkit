import{describe,it,expect}from"vitest";import{Pausable}from"../../clarity/stdlib/Pausable";
describe("Pausable",()=>{it("creates",()=>expect(new Pausable()).toBeDefined());it("state",()=>{const x=new Pausable();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new Pausable();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new Pausable();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new Pausable();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new Pausable();expect(x.toJSON().active).toBe(true);});});