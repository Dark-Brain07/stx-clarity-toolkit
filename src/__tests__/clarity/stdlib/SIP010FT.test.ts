import{describe,it,expect}from"vitest";import{SIP010FT}from"../../clarity/stdlib/SIP010FT";
describe("SIP010FT",()=>{it("creates",()=>expect(new SIP010FT()).toBeDefined());it("state",()=>{const x=new SIP010FT();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SIP010FT();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SIP010FT();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SIP010FT();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SIP010FT();expect(x.toJSON().active).toBe(true);});});