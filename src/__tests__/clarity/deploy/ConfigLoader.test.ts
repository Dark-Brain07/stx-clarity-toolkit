import{describe,it,expect}from"vitest";import{ConfigLoader}from"../../clarity/deploy/ConfigLoader";
describe("ConfigLoader",()=>{it("creates",()=>expect(new ConfigLoader()).toBeDefined());it("state",()=>{const x=new ConfigLoader();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ConfigLoader();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ConfigLoader();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ConfigLoader();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ConfigLoader();expect(x.toJSON().active).toBe(true);});});