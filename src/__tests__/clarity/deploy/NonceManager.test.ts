import{describe,it,expect}from"vitest";import{NonceManager}from"../../clarity/deploy/NonceManager";
describe("NonceManager",()=>{it("creates",()=>expect(new NonceManager()).toBeDefined());it("state",()=>{const x=new NonceManager();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NonceManager();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NonceManager();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NonceManager();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NonceManager();expect(x.toJSON().active).toBe(true);});});