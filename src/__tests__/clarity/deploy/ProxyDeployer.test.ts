import{describe,it,expect}from"vitest";import{ProxyDeployer}from"../../clarity/deploy/ProxyDeployer";
describe("ProxyDeployer",()=>{it("creates",()=>expect(new ProxyDeployer()).toBeDefined());it("state",()=>{const x=new ProxyDeployer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ProxyDeployer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ProxyDeployer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ProxyDeployer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ProxyDeployer();expect(x.toJSON().active).toBe(true);});});