import{describe,it,expect}from"vitest";import{DeployLogger}from"../../clarity/deploy/DeployLogger";
describe("DeployLogger",()=>{it("creates",()=>expect(new DeployLogger()).toBeDefined());it("state",()=>{const x=new DeployLogger();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DeployLogger();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DeployLogger();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DeployLogger();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DeployLogger();expect(x.toJSON().active).toBe(true);});});