import{describe,it,expect}from"vitest";import{RollbackHandler}from"../../clarity/deploy/RollbackHandler";
describe("RollbackHandler",()=>{it("creates",()=>expect(new RollbackHandler()).toBeDefined());it("state",()=>{const x=new RollbackHandler();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new RollbackHandler();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new RollbackHandler();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new RollbackHandler();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new RollbackHandler();expect(x.toJSON().active).toBe(true);});});