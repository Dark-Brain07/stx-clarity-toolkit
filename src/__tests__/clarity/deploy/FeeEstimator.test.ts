import{describe,it,expect}from"vitest";import{FeeEstimator}from"../../clarity/deploy/FeeEstimator";
describe("FeeEstimator",()=>{it("creates",()=>expect(new FeeEstimator()).toBeDefined());it("state",()=>{const x=new FeeEstimator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FeeEstimator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FeeEstimator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FeeEstimator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FeeEstimator();expect(x.toJSON().active).toBe(true);});});