import{describe,it,expect}from"vitest";import{ContractVerifier}from"../../clarity/deploy/ContractVerifier";
describe("ContractVerifier",()=>{it("creates",()=>expect(new ContractVerifier()).toBeDefined());it("state",()=>{const x=new ContractVerifier();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ContractVerifier();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ContractVerifier();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ContractVerifier();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ContractVerifier();expect(x.toJSON().active).toBe(true);});});