import{describe,it,expect}from"vitest";import{TxBroadcaster}from"../../clarity/deploy/TxBroadcaster";
describe("TxBroadcaster",()=>{it("creates",()=>expect(new TxBroadcaster()).toBeDefined());it("state",()=>{const x=new TxBroadcaster();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TxBroadcaster();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TxBroadcaster();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TxBroadcaster();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TxBroadcaster();expect(x.toJSON().active).toBe(true);});});