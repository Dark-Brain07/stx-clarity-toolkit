import{describe,it,expect}from"vitest";import{DeployPipeline}from"../../clarity/deploy/DeployPipeline";
describe("DeployPipeline",()=>{it("creates",()=>expect(new DeployPipeline()).toBeDefined());it("state",()=>{const x=new DeployPipeline();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DeployPipeline();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DeployPipeline();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DeployPipeline();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DeployPipeline();expect(x.toJSON().active).toBe(true);});});