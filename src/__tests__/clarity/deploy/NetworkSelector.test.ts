import{describe,it,expect}from"vitest";import{NetworkSelector}from"../../clarity/deploy/NetworkSelector";
describe("NetworkSelector",()=>{it("creates",()=>expect(new NetworkSelector()).toBeDefined());it("state",()=>{const x=new NetworkSelector();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NetworkSelector();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NetworkSelector();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NetworkSelector();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NetworkSelector();expect(x.toJSON().active).toBe(true);});});