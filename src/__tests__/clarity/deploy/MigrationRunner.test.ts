import{describe,it,expect}from"vitest";import{MigrationRunner}from"../../clarity/deploy/MigrationRunner";
describe("MigrationRunner",()=>{it("creates",()=>expect(new MigrationRunner()).toBeDefined());it("state",()=>{const x=new MigrationRunner();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MigrationRunner();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MigrationRunner();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MigrationRunner();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MigrationRunner();expect(x.toJSON().active).toBe(true);});});