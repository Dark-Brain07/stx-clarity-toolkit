import{describe,it,expect}from"vitest";import{DependencyResolver}from"../../clarity/deploy/DependencyResolver";
describe("DependencyResolver",()=>{it("creates",()=>expect(new DependencyResolver()).toBeDefined());it("state",()=>{const x=new DependencyResolver();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DependencyResolver();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DependencyResolver();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DependencyResolver();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DependencyResolver();expect(x.toJSON().active).toBe(true);});});