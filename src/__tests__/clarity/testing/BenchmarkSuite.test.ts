import{describe,it,expect}from"vitest";import{BenchmarkSuite}from"../../clarity/testing/BenchmarkSuite";
describe("BenchmarkSuite",()=>{it("creates",()=>expect(new BenchmarkSuite()).toBeDefined());it("state",()=>{const x=new BenchmarkSuite();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BenchmarkSuite();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BenchmarkSuite();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BenchmarkSuite();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BenchmarkSuite();expect(x.toJSON().active).toBe(true);});});