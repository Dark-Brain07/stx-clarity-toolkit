import{describe,it,expect}from"vitest";import{GasProfiler}from"../../clarity/testing/GasProfiler";
describe("GasProfiler",()=>{it("creates",()=>expect(new GasProfiler()).toBeDefined());it("state",()=>{const x=new GasProfiler();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new GasProfiler();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new GasProfiler();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new GasProfiler();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new GasProfiler();expect(x.toJSON().active).toBe(true);});});