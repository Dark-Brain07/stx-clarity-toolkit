import{describe,it,expect}from"vitest";import{EventCapture}from"../../clarity/testing/EventCapture";
describe("EventCapture",()=>{it("creates",()=>expect(new EventCapture()).toBeDefined());it("state",()=>{const x=new EventCapture();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new EventCapture();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new EventCapture();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new EventCapture();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new EventCapture();expect(x.toJSON().active).toBe(true);});});