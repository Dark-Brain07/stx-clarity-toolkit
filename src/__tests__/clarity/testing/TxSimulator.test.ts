import{describe,it,expect}from"vitest";import{TxSimulator}from"../../clarity/testing/TxSimulator";
describe("TxSimulator",()=>{it("creates",()=>expect(new TxSimulator()).toBeDefined());it("state",()=>{const x=new TxSimulator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TxSimulator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TxSimulator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TxSimulator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TxSimulator();expect(x.toJSON().active).toBe(true);});});