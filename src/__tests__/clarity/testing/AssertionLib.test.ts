import{describe,it,expect}from"vitest";import{AssertionLib}from"../../clarity/testing/AssertionLib";
describe("AssertionLib",()=>{it("creates",()=>expect(new AssertionLib()).toBeDefined());it("state",()=>{const x=new AssertionLib();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AssertionLib();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AssertionLib();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AssertionLib();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AssertionLib();expect(x.toJSON().active).toBe(true);});});