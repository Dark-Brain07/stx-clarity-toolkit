import{describe,it,expect}from"vitest";import{PropertyTest}from"../../clarity/testing/PropertyTest";
describe("PropertyTest",()=>{it("creates",()=>expect(new PropertyTest()).toBeDefined());it("state",()=>{const x=new PropertyTest();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PropertyTest();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PropertyTest();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PropertyTest();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PropertyTest();expect(x.toJSON().active).toBe(true);});});