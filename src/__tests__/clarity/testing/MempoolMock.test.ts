import{describe,it,expect}from"vitest";import{MempoolMock}from"../../clarity/testing/MempoolMock";
describe("MempoolMock",()=>{it("creates",()=>expect(new MempoolMock()).toBeDefined());it("state",()=>{const x=new MempoolMock();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MempoolMock();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MempoolMock();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MempoolMock();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MempoolMock();expect(x.toJSON().active).toBe(true);});});