import{describe,it,expect}from"vitest";import{CoverageTracker}from"../../clarity/testing/CoverageTracker";
describe("CoverageTracker",()=>{it("creates",()=>expect(new CoverageTracker()).toBeDefined());it("state",()=>{const x=new CoverageTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new CoverageTracker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new CoverageTracker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new CoverageTracker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new CoverageTracker();expect(x.toJSON().active).toBe(true);});});