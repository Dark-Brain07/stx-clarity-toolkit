import{describe,it,expect}from"vitest";import{SnapshotTest}from"../../clarity/testing/SnapshotTest";
describe("SnapshotTest",()=>{it("creates",()=>expect(new SnapshotTest()).toBeDefined());it("state",()=>{const x=new SnapshotTest();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SnapshotTest();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SnapshotTest();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SnapshotTest();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SnapshotTest();expect(x.toJSON().active).toBe(true);});});