import{describe,it,expect}from"vitest";import{LiquidityAnalytics}from"../../tools/analytics/LiquidityAnalytics";
describe("LiquidityAnalytics",()=>{it("creates",()=>expect(new LiquidityAnalytics()).toBeDefined());it("state",()=>{const x=new LiquidityAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LiquidityAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new LiquidityAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new LiquidityAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new LiquidityAnalytics();expect(x.toJSON().active).toBe(true);});});