import{describe,it,expect}from"vitest";import{YieldAnalytics}from"../../tools/analytics/YieldAnalytics";
describe("YieldAnalytics",()=>{it("creates",()=>expect(new YieldAnalytics()).toBeDefined());it("state",()=>{const x=new YieldAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new YieldAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new YieldAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new YieldAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new YieldAnalytics();expect(x.toJSON().active).toBe(true);});});