import{describe,it,expect}from"vitest";import{WalletAnalytics}from"../../tools/analytics/WalletAnalytics";
describe("WalletAnalytics",()=>{it("creates",()=>expect(new WalletAnalytics()).toBeDefined());it("state",()=>{const x=new WalletAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WalletAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WalletAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WalletAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WalletAnalytics();expect(x.toJSON().active).toBe(true);});});