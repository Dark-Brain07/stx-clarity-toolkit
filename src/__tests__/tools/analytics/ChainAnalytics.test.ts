import{describe,it,expect}from"vitest";import{ChainAnalytics}from"../../tools/analytics/ChainAnalytics";
describe("ChainAnalytics",()=>{it("creates",()=>expect(new ChainAnalytics()).toBeDefined());it("state",()=>{const x=new ChainAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ChainAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ChainAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ChainAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ChainAnalytics();expect(x.toJSON().active).toBe(true);});});