import{describe,it,expect}from"vitest";import{GasAnalytics}from"../../tools/analytics/GasAnalytics";
describe("GasAnalytics",()=>{it("creates",()=>expect(new GasAnalytics()).toBeDefined());it("state",()=>{const x=new GasAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new GasAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new GasAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new GasAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new GasAnalytics();expect(x.toJSON().active).toBe(true);});});