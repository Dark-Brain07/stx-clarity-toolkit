import{describe,it,expect}from"vitest";import{ProtocolAnalytics}from"../../tools/analytics/ProtocolAnalytics";
describe("ProtocolAnalytics",()=>{it("creates",()=>expect(new ProtocolAnalytics()).toBeDefined());it("state",()=>{const x=new ProtocolAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ProtocolAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ProtocolAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ProtocolAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ProtocolAnalytics();expect(x.toJSON().active).toBe(true);});});