import{describe,it,expect}from"vitest";import{NFTAnalytics}from"../../tools/analytics/NFTAnalytics";
describe("NFTAnalytics",()=>{it("creates",()=>expect(new NFTAnalytics()).toBeDefined());it("state",()=>{const x=new NFTAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NFTAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NFTAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NFTAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NFTAnalytics();expect(x.toJSON().active).toBe(true);});});