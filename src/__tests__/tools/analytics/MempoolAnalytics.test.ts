import{describe,it,expect}from"vitest";import{MempoolAnalytics}from"../../tools/analytics/MempoolAnalytics";
describe("MempoolAnalytics",()=>{it("creates",()=>expect(new MempoolAnalytics()).toBeDefined());it("state",()=>{const x=new MempoolAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new MempoolAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new MempoolAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new MempoolAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new MempoolAnalytics();expect(x.toJSON().active).toBe(true);});});