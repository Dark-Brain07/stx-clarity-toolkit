import{describe,it,expect}from"vitest";import{DeFiAnalytics}from"../../tools/analytics/DeFiAnalytics";
describe("DeFiAnalytics",()=>{it("creates",()=>expect(new DeFiAnalytics()).toBeDefined());it("state",()=>{const x=new DeFiAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DeFiAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DeFiAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DeFiAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DeFiAnalytics();expect(x.toJSON().active).toBe(true);});});