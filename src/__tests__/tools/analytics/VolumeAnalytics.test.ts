import{describe,it,expect}from"vitest";import{VolumeAnalytics}from"../../tools/analytics/VolumeAnalytics";
describe("VolumeAnalytics",()=>{it("creates",()=>expect(new VolumeAnalytics()).toBeDefined());it("state",()=>{const x=new VolumeAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new VolumeAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new VolumeAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new VolumeAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new VolumeAnalytics();expect(x.toJSON().active).toBe(true);});});