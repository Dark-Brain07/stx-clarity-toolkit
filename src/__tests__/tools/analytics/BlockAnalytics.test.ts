import{describe,it,expect}from"vitest";import{BlockAnalytics}from"../../tools/analytics/BlockAnalytics";
describe("BlockAnalytics",()=>{it("creates",()=>expect(new BlockAnalytics()).toBeDefined());it("state",()=>{const x=new BlockAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BlockAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BlockAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BlockAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BlockAnalytics();expect(x.toJSON().active).toBe(true);});});