import{describe,it,expect}from"vitest";import{WhaleMonitor}from"../../tools/monitoring/WhaleMonitor";
describe("WhaleMonitor",()=>{it("creates",()=>expect(new WhaleMonitor()).toBeDefined());it("state",()=>{const x=new WhaleMonitor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WhaleMonitor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WhaleMonitor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WhaleMonitor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WhaleMonitor();expect(x.toJSON().active).toBe(true);});});