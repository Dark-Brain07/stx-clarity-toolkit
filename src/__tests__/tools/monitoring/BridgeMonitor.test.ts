import{describe,it,expect}from"vitest";import{BridgeMonitor}from"../../tools/monitoring/BridgeMonitor";
describe("BridgeMonitor",()=>{it("creates",()=>expect(new BridgeMonitor()).toBeDefined());it("state",()=>{const x=new BridgeMonitor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BridgeMonitor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BridgeMonitor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BridgeMonitor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BridgeMonitor();expect(x.toJSON().active).toBe(true);});});