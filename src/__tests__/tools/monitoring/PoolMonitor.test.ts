import{describe,it,expect}from"vitest";import{PoolMonitor}from"../../tools/monitoring/PoolMonitor";
describe("PoolMonitor",()=>{it("creates",()=>expect(new PoolMonitor()).toBeDefined());it("state",()=>{const x=new PoolMonitor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PoolMonitor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PoolMonitor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PoolMonitor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PoolMonitor();expect(x.toJSON().active).toBe(true);});});