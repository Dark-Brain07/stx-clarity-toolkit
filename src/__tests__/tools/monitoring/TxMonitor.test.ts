import{describe,it,expect}from"vitest";import{TxMonitor}from"../../tools/monitoring/TxMonitor";
describe("TxMonitor",()=>{it("creates",()=>expect(new TxMonitor()).toBeDefined());it("state",()=>{const x=new TxMonitor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TxMonitor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TxMonitor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TxMonitor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TxMonitor();expect(x.toJSON().active).toBe(true);});});