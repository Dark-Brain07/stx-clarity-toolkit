import{describe,it,expect}from"vitest";import{LatencyTracker}from"../../tools/monitoring/LatencyTracker";
describe("LatencyTracker",()=>{it("creates",()=>expect(new LatencyTracker()).toBeDefined());it("state",()=>{const x=new LatencyTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new LatencyTracker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new LatencyTracker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new LatencyTracker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new LatencyTracker();expect(x.toJSON().active).toBe(true);});});