import{describe,it,expect}from"vitest";import{ErrorTracker}from"../../tools/monitoring/ErrorTracker";
describe("ErrorTracker",()=>{it("creates",()=>expect(new ErrorTracker()).toBeDefined());it("state",()=>{const x=new ErrorTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ErrorTracker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ErrorTracker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ErrorTracker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ErrorTracker();expect(x.toJSON().active).toBe(true);});});