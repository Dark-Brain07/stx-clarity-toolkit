import{describe,it,expect}from"vitest";import{SecurityMonitor}from"../../tools/monitoring/SecurityMonitor";
describe("SecurityMonitor",()=>{it("creates",()=>expect(new SecurityMonitor()).toBeDefined());it("state",()=>{const x=new SecurityMonitor();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new SecurityMonitor();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new SecurityMonitor();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new SecurityMonitor();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new SecurityMonitor();expect(x.toJSON().active).toBe(true);});});