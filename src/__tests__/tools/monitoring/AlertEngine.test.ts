import{describe,it,expect}from"vitest";import{AlertEngine}from"../../tools/monitoring/AlertEngine";
describe("AlertEngine",()=>{it("creates",()=>expect(new AlertEngine()).toBeDefined());it("state",()=>{const x=new AlertEngine();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AlertEngine();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AlertEngine();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AlertEngine();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AlertEngine();expect(x.toJSON().active).toBe(true);});});