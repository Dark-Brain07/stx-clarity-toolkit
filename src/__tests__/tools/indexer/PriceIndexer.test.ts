import{describe,it,expect}from"vitest";import{PriceIndexer}from"../../tools/indexer/PriceIndexer";
describe("PriceIndexer",()=>{it("creates",()=>expect(new PriceIndexer()).toBeDefined());it("state",()=>{const x=new PriceIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PriceIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PriceIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PriceIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PriceIndexer();expect(x.toJSON().active).toBe(true);});});