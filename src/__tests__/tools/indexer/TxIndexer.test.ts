import{describe,it,expect}from"vitest";import{TxIndexer}from"../../tools/indexer/TxIndexer";
describe("TxIndexer",()=>{it("creates",()=>expect(new TxIndexer()).toBeDefined());it("state",()=>{const x=new TxIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TxIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TxIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TxIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TxIndexer();expect(x.toJSON().active).toBe(true);});});