import{describe,it,expect}from"vitest";import{StakeIndexer}from"../../tools/indexer/StakeIndexer";
describe("StakeIndexer",()=>{it("creates",()=>expect(new StakeIndexer()).toBeDefined());it("state",()=>{const x=new StakeIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new StakeIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new StakeIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new StakeIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new StakeIndexer();expect(x.toJSON().active).toBe(true);});});