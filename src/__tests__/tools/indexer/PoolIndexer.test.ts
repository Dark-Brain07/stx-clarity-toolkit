import{describe,it,expect}from"vitest";import{PoolIndexer}from"../../tools/indexer/PoolIndexer";
describe("PoolIndexer",()=>{it("creates",()=>expect(new PoolIndexer()).toBeDefined());it("state",()=>{const x=new PoolIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new PoolIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new PoolIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new PoolIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new PoolIndexer();expect(x.toJSON().active).toBe(true);});});