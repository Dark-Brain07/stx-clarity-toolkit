import{describe,it,expect}from"vitest";import{FeeIndexer}from"../../tools/indexer/FeeIndexer";
describe("FeeIndexer",()=>{it("creates",()=>expect(new FeeIndexer()).toBeDefined());it("state",()=>{const x=new FeeIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FeeIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FeeIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FeeIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FeeIndexer();expect(x.toJSON().active).toBe(true);});});