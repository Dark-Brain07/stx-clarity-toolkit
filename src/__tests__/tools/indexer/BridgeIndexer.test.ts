import{describe,it,expect}from"vitest";import{BridgeIndexer}from"../../tools/indexer/BridgeIndexer";
describe("BridgeIndexer",()=>{it("creates",()=>expect(new BridgeIndexer()).toBeDefined());it("state",()=>{const x=new BridgeIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BridgeIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BridgeIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BridgeIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BridgeIndexer();expect(x.toJSON().active).toBe(true);});});