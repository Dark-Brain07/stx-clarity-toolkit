import{describe,it,expect}from"vitest";import{ContractIndexer}from"../../tools/indexer/ContractIndexer";
describe("ContractIndexer",()=>{it("creates",()=>expect(new ContractIndexer()).toBeDefined());it("state",()=>{const x=new ContractIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ContractIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ContractIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ContractIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ContractIndexer();expect(x.toJSON().active).toBe(true);});});