import{describe,it,expect}from"vitest";import{TokenIndexer}from"../../tools/indexer/TokenIndexer";
describe("TokenIndexer",()=>{it("creates",()=>expect(new TokenIndexer()).toBeDefined());it("state",()=>{const x=new TokenIndexer();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new TokenIndexer();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new TokenIndexer();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new TokenIndexer();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new TokenIndexer();expect(x.toJSON().active).toBe(true);});});