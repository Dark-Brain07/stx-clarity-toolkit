import{describe,it,expect}from"vitest";import{FTPortfolio}from"../../tools/wallet/FTPortfolio";
describe("FTPortfolio",()=>{it("creates",()=>expect(new FTPortfolio()).toBeDefined());it("state",()=>{const x=new FTPortfolio();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FTPortfolio();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FTPortfolio();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FTPortfolio();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FTPortfolio();expect(x.toJSON().active).toBe(true);});});