import{describe,it,expect}from"vitest";import{DelegationMgr}from"../../tools/wallet/DelegationMgr";
describe("DelegationMgr",()=>{it("creates",()=>expect(new DelegationMgr()).toBeDefined());it("state",()=>{const x=new DelegationMgr();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DelegationMgr();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DelegationMgr();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DelegationMgr();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DelegationMgr();expect(x.toJSON().active).toBe(true);});});