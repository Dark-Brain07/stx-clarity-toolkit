import{describe,it,expect}from"vitest";import{WalletScanner}from"../../tools/wallet/WalletScanner";
describe("WalletScanner",()=>{it("creates",()=>expect(new WalletScanner()).toBeDefined());it("state",()=>{const x=new WalletScanner();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WalletScanner();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WalletScanner();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WalletScanner();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WalletScanner();expect(x.toJSON().active).toBe(true);});});