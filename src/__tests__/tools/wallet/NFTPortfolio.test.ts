import{describe,it,expect}from"vitest";import{NFTPortfolio}from"../../tools/wallet/NFTPortfolio";
describe("NFTPortfolio",()=>{it("creates",()=>expect(new NFTPortfolio()).toBeDefined());it("state",()=>{const x=new NFTPortfolio();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NFTPortfolio();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NFTPortfolio();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NFTPortfolio();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NFTPortfolio();expect(x.toJSON().active).toBe(true);});});