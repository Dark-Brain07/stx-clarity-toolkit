import{describe,it,expect}from"vitest";import{RewardCalc}from"../../tools/wallet/RewardCalc";
describe("RewardCalc",()=>{it("creates",()=>expect(new RewardCalc()).toBeDefined());it("state",()=>{const x=new RewardCalc();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new RewardCalc();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new RewardCalc();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new RewardCalc();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new RewardCalc();expect(x.toJSON().active).toBe(true);});});