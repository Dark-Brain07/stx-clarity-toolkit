import{describe,it,expect}from"vitest";import{AddressBook}from"../../tools/wallet/AddressBook";
describe("AddressBook",()=>{it("creates",()=>expect(new AddressBook()).toBeDefined());it("state",()=>{const x=new AddressBook();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AddressBook();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AddressBook();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AddressBook();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AddressBook();expect(x.toJSON().active).toBe(true);});});