import{describe,it,expect}from"vitest";import{VotingTracker}from"../../tools/wallet/VotingTracker";
describe("VotingTracker",()=>{it("creates",()=>expect(new VotingTracker()).toBeDefined());it("state",()=>{const x=new VotingTracker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new VotingTracker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new VotingTracker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new VotingTracker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new VotingTracker();expect(x.toJSON().active).toBe(true);});});