import{describe,it,expect}from"vitest";import{WhaleDetector}from"../../tools/wallet/WhaleDetector";
describe("WhaleDetector",()=>{it("creates",()=>expect(new WhaleDetector()).toBeDefined());it("state",()=>{const x=new WhaleDetector();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WhaleDetector();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WhaleDetector();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WhaleDetector();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WhaleDetector();expect(x.toJSON().active).toBe(true);});});