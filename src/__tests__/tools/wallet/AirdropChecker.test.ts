import{describe,it,expect}from"vitest";import{AirdropChecker}from"../../tools/wallet/AirdropChecker";
describe("AirdropChecker",()=>{it("creates",()=>expect(new AirdropChecker()).toBeDefined());it("state",()=>{const x=new AirdropChecker();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AirdropChecker();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AirdropChecker();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AirdropChecker();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AirdropChecker();expect(x.toJSON().active).toBe(true);});});