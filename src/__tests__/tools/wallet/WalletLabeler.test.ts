import{describe,it,expect}from"vitest";import{WalletLabeler}from"../../tools/wallet/WalletLabeler";
describe("WalletLabeler",()=>{it("creates",()=>expect(new WalletLabeler()).toBeDefined());it("state",()=>{const x=new WalletLabeler();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WalletLabeler();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WalletLabeler();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WalletLabeler();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WalletLabeler();expect(x.toJSON().active).toBe(true);});});