import{describe,it,expect}from"vitest";import{BalanceAggregator}from"../../tools/wallet/BalanceAggregator";
describe("BalanceAggregator",()=>{it("creates",()=>expect(new BalanceAggregator()).toBeDefined());it("state",()=>{const x=new BalanceAggregator();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new BalanceAggregator();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new BalanceAggregator();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new BalanceAggregator();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new BalanceAggregator();expect(x.toJSON().active).toBe(true);});});