import{describe,it,expect}from"vitest";import{NetworkProvider}from"../../react/providers/NetworkProvider";
describe("NetworkProvider",()=>{it("creates",()=>expect(new NetworkProvider()).toBeDefined());it("state",()=>{const x=new NetworkProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NetworkProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NetworkProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NetworkProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NetworkProvider();expect(x.toJSON().active).toBe(true);});});