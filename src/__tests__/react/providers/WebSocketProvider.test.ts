import{describe,it,expect}from"vitest";import{WebSocketProvider}from"../../react/providers/WebSocketProvider";
describe("WebSocketProvider",()=>{it("creates",()=>expect(new WebSocketProvider()).toBeDefined());it("state",()=>{const x=new WebSocketProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WebSocketProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WebSocketProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WebSocketProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WebSocketProvider();expect(x.toJSON().active).toBe(true);});});