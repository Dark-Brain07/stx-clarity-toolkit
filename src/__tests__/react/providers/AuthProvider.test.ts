import{describe,it,expect}from"vitest";import{AuthProvider}from"../../react/providers/AuthProvider";
describe("AuthProvider",()=>{it("creates",()=>expect(new AuthProvider()).toBeDefined());it("state",()=>{const x=new AuthProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new AuthProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new AuthProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new AuthProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new AuthProvider();expect(x.toJSON().active).toBe(true);});});