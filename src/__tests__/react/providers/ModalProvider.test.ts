import{describe,it,expect}from"vitest";import{ModalProvider}from"../../react/providers/ModalProvider";
describe("ModalProvider",()=>{it("creates",()=>expect(new ModalProvider()).toBeDefined());it("state",()=>{const x=new ModalProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ModalProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ModalProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ModalProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ModalProvider();expect(x.toJSON().active).toBe(true);});});