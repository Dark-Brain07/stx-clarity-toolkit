import{describe,it,expect}from"vitest";import{ToastProvider}from"../../react/providers/ToastProvider";
describe("ToastProvider",()=>{it("creates",()=>expect(new ToastProvider()).toBeDefined());it("state",()=>{const x=new ToastProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ToastProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ToastProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ToastProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ToastProvider();expect(x.toJSON().active).toBe(true);});});