import{describe,it,expect}from"vitest";import{CacheProvider}from"../../react/providers/CacheProvider";
describe("CacheProvider",()=>{it("creates",()=>expect(new CacheProvider()).toBeDefined());it("state",()=>{const x=new CacheProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new CacheProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new CacheProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new CacheProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new CacheProvider();expect(x.toJSON().active).toBe(true);});});