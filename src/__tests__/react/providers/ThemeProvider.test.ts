import{describe,it,expect}from"vitest";import{ThemeProvider}from"../../react/providers/ThemeProvider";
describe("ThemeProvider",()=>{it("creates",()=>expect(new ThemeProvider()).toBeDefined());it("state",()=>{const x=new ThemeProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ThemeProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ThemeProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ThemeProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ThemeProvider();expect(x.toJSON().active).toBe(true);});});