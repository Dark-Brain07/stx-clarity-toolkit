import{describe,it,expect}from"vitest";import{WalletProvider}from"../../react/providers/WalletProvider";
describe("WalletProvider",()=>{it("creates",()=>expect(new WalletProvider()).toBeDefined());it("state",()=>{const x=new WalletProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new WalletProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new WalletProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new WalletProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new WalletProvider();expect(x.toJSON().active).toBe(true);});});