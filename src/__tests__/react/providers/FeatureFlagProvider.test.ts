import{describe,it,expect}from"vitest";import{FeatureFlagProvider}from"../../react/providers/FeatureFlagProvider";
describe("FeatureFlagProvider",()=>{it("creates",()=>expect(new FeatureFlagProvider()).toBeDefined());it("state",()=>{const x=new FeatureFlagProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new FeatureFlagProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new FeatureFlagProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new FeatureFlagProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new FeatureFlagProvider();expect(x.toJSON().active).toBe(true);});});