import{describe,it,expect}from"vitest";import{ErrorBoundary}from"../../react/providers/ErrorBoundary";
describe("ErrorBoundary",()=>{it("creates",()=>expect(new ErrorBoundary()).toBeDefined());it("state",()=>{const x=new ErrorBoundary();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new ErrorBoundary();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new ErrorBoundary();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new ErrorBoundary();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new ErrorBoundary();expect(x.toJSON().active).toBe(true);});});