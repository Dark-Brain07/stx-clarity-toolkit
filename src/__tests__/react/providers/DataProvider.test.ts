import{describe,it,expect}from"vitest";import{DataProvider}from"../../react/providers/DataProvider";
describe("DataProvider",()=>{it("creates",()=>expect(new DataProvider()).toBeDefined());it("state",()=>{const x=new DataProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new DataProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new DataProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new DataProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new DataProvider();expect(x.toJSON().active).toBe(true);});});