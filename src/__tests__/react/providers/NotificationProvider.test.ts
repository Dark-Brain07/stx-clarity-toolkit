import{describe,it,expect}from"vitest";import{NotificationProvider}from"../../react/providers/NotificationProvider";
describe("NotificationProvider",()=>{it("creates",()=>expect(new NotificationProvider()).toBeDefined());it("state",()=>{const x=new NotificationProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new NotificationProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new NotificationProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new NotificationProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new NotificationProvider();expect(x.toJSON().active).toBe(true);});});