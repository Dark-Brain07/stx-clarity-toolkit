import{describe,it,expect}from"vitest";import{StacksProvider}from"../../react/providers/StacksProvider";
describe("StacksProvider",()=>{it("creates",()=>expect(new StacksProvider()).toBeDefined());it("state",()=>{const x=new StacksProvider();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new StacksProvider();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new StacksProvider();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new StacksProvider();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new StacksProvider();expect(x.toJSON().active).toBe(true);});});