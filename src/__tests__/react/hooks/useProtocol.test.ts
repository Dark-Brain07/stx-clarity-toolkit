import{describe,it,expect}from"vitest";import{useProtocol}from"../../react/hooks/useProtocol";
describe("useProtocol",()=>{it("creates",()=>expect(new useProtocol()).toBeDefined());it("state",()=>{const x=new useProtocol();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useProtocol();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useProtocol();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useProtocol();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useProtocol();expect(x.toJSON().active).toBe(true);});});