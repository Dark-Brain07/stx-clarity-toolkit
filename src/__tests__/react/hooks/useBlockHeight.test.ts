import{describe,it,expect}from"vitest";import{useBlockHeight}from"../../react/hooks/useBlockHeight";
describe("useBlockHeight",()=>{it("creates",()=>expect(new useBlockHeight()).toBeDefined());it("state",()=>{const x=new useBlockHeight();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useBlockHeight();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useBlockHeight();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useBlockHeight();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useBlockHeight();expect(x.toJSON().active).toBe(true);});});