import{describe,it,expect}from"vitest";import{useMempool}from"../../react/hooks/useMempool";
describe("useMempool",()=>{it("creates",()=>expect(new useMempool()).toBeDefined());it("state",()=>{const x=new useMempool();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useMempool();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useMempool();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useMempool();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useMempool();expect(x.toJSON().active).toBe(true);});});