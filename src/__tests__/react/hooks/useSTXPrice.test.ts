import{describe,it,expect}from"vitest";import{useSTXPrice}from"../../react/hooks/useSTXPrice";
describe("useSTXPrice",()=>{it("creates",()=>expect(new useSTXPrice()).toBeDefined());it("state",()=>{const x=new useSTXPrice();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useSTXPrice();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useSTXPrice();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useSTXPrice();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useSTXPrice();expect(x.toJSON().active).toBe(true);});});