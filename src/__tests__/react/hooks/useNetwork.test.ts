import{describe,it,expect}from"vitest";import{useNetwork}from"../../react/hooks/useNetwork";
describe("useNetwork",()=>{it("creates",()=>expect(new useNetwork()).toBeDefined());it("state",()=>{const x=new useNetwork();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useNetwork();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useNetwork();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useNetwork();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useNetwork();expect(x.toJSON().active).toBe(true);});});