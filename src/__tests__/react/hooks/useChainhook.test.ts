import{describe,it,expect}from"vitest";import{useChainhook}from"../../react/hooks/useChainhook";
describe("useChainhook",()=>{it("creates",()=>expect(new useChainhook()).toBeDefined());it("state",()=>{const x=new useChainhook();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useChainhook();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useChainhook();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useChainhook();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useChainhook();expect(x.toJSON().active).toBe(true);});});