import{describe,it,expect}from"vitest";import{useWallet}from"../../react/hooks/useWallet";
describe("useWallet",()=>{it("creates",()=>expect(new useWallet()).toBeDefined());it("state",()=>{const x=new useWallet();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useWallet();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useWallet();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useWallet();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useWallet();expect(x.toJSON().active).toBe(true);});});