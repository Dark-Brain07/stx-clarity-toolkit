import{describe,it,expect}from"vitest";import{useAnalytics}from"../../react/hooks/useAnalytics";
describe("useAnalytics",()=>{it("creates",()=>expect(new useAnalytics()).toBeDefined());it("state",()=>{const x=new useAnalytics();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useAnalytics();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useAnalytics();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useAnalytics();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useAnalytics();expect(x.toJSON().active).toBe(true);});});