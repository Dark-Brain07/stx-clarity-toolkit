import{describe,it,expect}from"vitest";import{useContract}from"../../react/hooks/useContract";
describe("useContract",()=>{it("creates",()=>expect(new useContract()).toBeDefined());it("state",()=>{const x=new useContract();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useContract();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useContract();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useContract();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useContract();expect(x.toJSON().active).toBe(true);});});