import{describe,it,expect}from"vitest";import{useBalance}from"../../react/hooks/useBalance";
describe("useBalance",()=>{it("creates",()=>expect(new useBalance()).toBeDefined());it("state",()=>{const x=new useBalance();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useBalance();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useBalance();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useBalance();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useBalance();expect(x.toJSON().active).toBe(true);});});