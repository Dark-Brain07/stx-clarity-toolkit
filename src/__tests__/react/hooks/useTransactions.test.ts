import{describe,it,expect}from"vitest";import{useTransactions}from"../../react/hooks/useTransactions";
describe("useTransactions",()=>{it("creates",()=>expect(new useTransactions()).toBeDefined());it("state",()=>{const x=new useTransactions();x.set("k",1);expect(x.get("k")).toBe(1);});it("toggle",()=>{const x=new useTransactions();x.disable();expect(x.isActive()).toBe(false);});it("reset",()=>{const x=new useTransactions();x.set("a",1);x.reset();expect(x.has("a")).toBe(false);});it("clone",()=>{const x=new useTransactions();x.set("v",42);const c=x.clone();expect(c.get("v")).toBe(42);});it("json",()=>{const x=new useTransactions();expect(x.toJSON().active).toBe(true);});});